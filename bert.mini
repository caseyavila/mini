struct node
{
   int data;
   struct node next;
   struct point ad;
};

struct point
{
   int next;
   int y;
};

struct point hi;
bool deez;

fun main() int
{
   int i, element;
   struct node myList, copyList1, copyList2, sortedList;

   myList.next.next.next.data = i;

   i = i + 1;

   return 0;
}

fun man(int hi, struct point h) struct point {
    int hid;
}
